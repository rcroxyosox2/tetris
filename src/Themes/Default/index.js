const colors = {
  white: '#fff',
  black: '#444'
};

const gameBoard = {
  backgroundColor: colors.black
}

const matrix = {
  border: {
    inner: `1px solid ${colors.white}`
  }
}

export default {
  colors,
  gameBoard,
  matrix
}