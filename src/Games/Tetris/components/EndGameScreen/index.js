import React from 'react';
import * as styles from './styles';

export default class EndGameScreen extends React.PureComponent {
  render() {
    return (
      <styles.EndGameScreen>
        Game over biatch
      </styles.EndGameScreen>
    );
  }
}